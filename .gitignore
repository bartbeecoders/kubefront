# Rust
/src-tauri/target/
target/

# Tauri generated (schemas, etc.)
/src-tauri/gen/

# Node / frontend
node_modules/
dist/
.vite/

# Use npm (package-lock.json); don't track other lockfiles
pnpm-lock.yaml
yarn.lock

# Logs / env
*.log
.env
.DS_Store

# Local dev backend config generated by scripts/run.sh (host-specific paths)
/backend.toml

# Secrets — never commit credentials or signing keys
Vibecoding/kubeconfig
*.kubeconfig
scripts/certs/*
!scripts/certs/.gitkeep

testrun/
